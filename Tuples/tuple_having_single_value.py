#tuple is immutable
#the syntax of tuple is : tupel=();
#the syntax of list is :list=[];
#we can convert list tuple and vice versa.
tuple1 = (1,);
#tuple having single value must include the ,(commad) at the end
print(tuple1);
#an empty list can be written as the T1=();
